# multi-service-debug-sandbox
This sandbox solves the real pain point: developers can now debug a realistic 10-20 service environment locally without fighting over shared test infrastructure, and see exactly where requests are failing across service boundaries.
